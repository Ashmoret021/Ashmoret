name: CI

on:
  push:
    branches:
      - main
    tags:
      - '*'

jobs:
  pipeline:
    uses: Devops-Had-Hatzot/ci-template/.github/workflows/main.yml@main
    with:
      TAG: "latest"
      MADOR: "ashmoret"
    secrets: inherit
